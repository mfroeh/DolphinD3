<Window x:Class="Dolphin.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dolphin.UI"
        mc:Ignorable="d"
        Title="MainWindow" ResizeMode="CanMinimize" Width="800" Height="400">
    <Window.Style>
        <Style TargetType="Window" BasedOn="{StaticResource {x:Type Window}}"/>
    </Window.Style>

    <TabControl ItemsSource="{Binding Tabs}" SelectedIndex="{Binding Selected}">
        <TabControl.ItemContainerStyle>
            <Style TargetType="{x:Type TabItem}">
                <Setter Property="Header" Value="{Binding Title}" />
            </Style>
        </TabControl.ItemContainerStyle>
        <TabControl.Resources>
            <DataTemplate DataType="{x:Type local:DataTabViewModel}">
                <local:DataTab></local:DataTab>
            </DataTemplate>
            <DataTemplate DataType="{x:Type local:LogTabViewModel}">
                <local:LogTab></local:LogTab>
            </DataTemplate>
        </TabControl.Resources>
    </TabControl>
</Window>
